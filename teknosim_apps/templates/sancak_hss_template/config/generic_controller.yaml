controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    sancak_velocity_controller:
      type: velocity_controllers/JointGroupVelocityController

sancak_velocity_controller:
  ros__parameters:
    joints:
      - rotor1_joint
      - rotor2_joint
      - rotor3_joint
      - rotor4_joint
